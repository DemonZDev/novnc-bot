services:
  - type: web
    name: novnc-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && playwright install chromium"
    startCommand: "python bot.py"
    envVars:
      - key: PORT
        value: 10000
